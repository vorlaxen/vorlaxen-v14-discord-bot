# Docker
docker compose -f docker-compose.dev.yml up -d --build
docker compose -f docker-compose.prod.yml up -d --build

docker exec -it app_postgres psql -U vdb -d mektupx-api
docker compose ps

# ZIP TAR
tar --exclude='node_modules' --exclude='archive' --exclude='.git' -cvf archive/deploy0.tar .